#端口
server:
  port: 9200

#服务名称
spring:
  application:
      name: new-api-gateway
  cloud:
    nacos:
      discovery:
        server-addr:  127.0.0.1:8848    

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
 

 