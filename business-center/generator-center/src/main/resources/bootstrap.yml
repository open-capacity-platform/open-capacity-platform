#端口
server:
  port: 6503   #固定端口
#服务名称 
spring:
  application:
    name: generator-center
  cloud:
    nacos:
      discovery:
        server-addr:  127.0.0.1:8848  

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always     