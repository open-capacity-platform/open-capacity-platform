#端口配置
server:
  port: 4000
#服务名称
spring:
  application:
    name: sms-center
  cloud:
    nacos:
      discovery:
        server-addr:  127.0.0.1:8848  

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always     